# Monetra frontend staging environment.
# Copy this file to `frontend/.env` only if running the frontend outside Docker.
# Docker Compose passes API_PROXY_TARGET directly during build/runtime.

API_PROXY_TARGET=http://backend:8000
